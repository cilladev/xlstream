# Rust
/target
**/target
Cargo.lock.bak
*.rs.bk
*.orig

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
build/
dist/
wheels/
*.whl

# maturin
/bindings/python/target
/bindings/python/*.so
/bindings/python/*.pyd

# IDE
.idea/
.vscode/
*.iml

# macOS / Windows
.DS_Store
Thumbs.db

# Local config
.env
.envrc

# xlsx files — generated, never committed (too large, binary)
*.xlsx

# Benchmark artefacts
/benchmarks/results/*.json
/benchmarks/results/*.html

# Coverage
tarpaulin-report.html
/lcov.info


NOTEBOOK.md
MANAGER.md

# Upstream library source, cloned for agent / dev reference only. Never
# committed. Recreate with the clone commands in CLAUDE.md (or just
# `git clone` manually; the list is small).
refs/

# Per-phase implementation plans written by the dev agent. Ephemeral —
# they describe intent at phase-kickoff time and rot the moment the phase
# completes. Real decisions go in docs/decisions/ (ADRs). Real design goes
# in docs/architecture/.
docs/superpowers/

.claude

# Git worktrees
.worktrees/
