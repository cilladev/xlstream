# Phase 12 — Benchmarks

**Goal:** automated benchmark harness with reference workloads, criterion micro-benches, and public perf tracking.

**Estimated effort:** 3–4 days.

**Prerequisites:** Phase 11 complete (so end-to-end works).

**Reading:** [`docs/research/benchmarks.md`](../research/benchmarks.md).

**Output:** `cargo bench` produces HTML report. Nightly CI publishes results to gh-pages. Reference workload hits targets.

## Workload tiers

### Tier sizes

| Tier | Rows | Total cols | Data cols | Formula cols | Purpose |
|---|---|---|---|---|---|
| Small | 10,000 | 50 | 20 | 30 | Per-PR smoke (< 30s) |
| Medium | 100,000 | 50 | 20 | 30 | Nightly regression |
| Large | 1,000,000 | 50 | 30 | 30 | Full benchmark, release gate |

### Formula column coverage (30 formula cols)

Every formula category is represented. Each column exercises a different builtin or operator.

| Cols | Formula pattern | Category |
|---|---|---|
| 1 | `=A+B` | Arithmetic: add |
| 2 | `=C-D` | Arithmetic: subtract |
| 3 | `=E*F` | Arithmetic: multiply |
| 4 | `=G/H` | Arithmetic: divide |
| 5 | `=I^2` | Arithmetic: power |
| 6 | `=-J` | Unary: negate |
| 7 | `=K%` | Unary: percent |
| 8 | `=L&M` | String: concat operator |
| 9 | `=N>O` | Comparison: greater than |
| 10 | `=P=Q` | Comparison: equals |
| 11 | `=IF(A>0, B, C)` | Conditional: IF |
| 12 | `=IFS(A>100, "P", A>50, "G", TRUE, "B")` | Conditional: IFS |
| 13 | `=AND(A>0, B>0)` | Logical: AND |
| 14 | `=IFERROR(G/H, 0)` | Logical: IFERROR |
| 15 | `=SUM(col)` | Aggregate: SUM (prelude) |
| 16 | `=AVERAGE(col)` | Aggregate: AVERAGE (prelude) |
| 17 | `=SUMIF(col, "X", col)` | Conditional aggregate: SUMIF |
| 18 | `=COUNTIF(col, ">0")` | Conditional aggregate: COUNTIF |
| 19 | `=VLOOKUP(A, Lookup!A:C, 2, FALSE)` | Lookup: VLOOKUP exact |
| 20 | `=INDEX(Lookup!B:B, MATCH(A, Lookup!A:A, 0))` | Lookup: INDEX/MATCH |
| 21 | `=LEFT(L, 3)` | String: LEFT |
| 22 | `=UPPER(M)` | String: UPPER |
| 23 | `=ROUND(E, 2)` | Math: ROUND |
| 24 | `=MOD(F, G)` | Math: MOD |
| 25 | `=YEAR(date_col)` | Date: YEAR |
| 26 | `=EDATE(date_col, 3)` | Date: EDATE |
| 27 | `=ISNUMBER(A)` | Info: ISNUMBER |
| 28 | `=TYPE(B)` | Info: TYPE |
| 29 | `=TEXT(E, "0.00")` | Conversion: TEXT |
| 30 | `=VALUE("123")` | Conversion: VALUE |

### Lookup sheets

Each generated workbook includes:
- **Lookup1** — 1,000 rows × 4 cols (region/code/rate/key). For VLOOKUP/INDEX/MATCH.
- **Lookup2** — 10,000 rows × 3 cols (product/category/price). Stress test for larger lookup tables.
- **Holidays** — 30 rows × 1 col (date serials). For NETWORKDAYS/WORKDAY.

### Performance targets

| Metric | Small (10k) | Medium (100k) | Large (1M) |
|---|---|---|---|
| Wall-clock (1 core) | < 2s | < 20s | < 200s |
| Wall-clock (8 cores) | < 1s | < 5s | < 40s |
| Peak RSS | < 50 MB | < 100 MB | < 300 MB |

### Comparative baseline

| Engine | Workload | Wall-clock | Peak RSS |
|---|---|---|---|
| **formualizer** (measured 2026-04-17) | 400k × 20 | 5h 40m | 3.3 GB |
| **xlstream single-threaded** (measured 2026-04-19) | 700k × 20 | 48s | 734 MB |
| **xlstream 8-core** (target) | 700k × 20 | < 15s | < 800 MB |
| **xlstream single-threaded** (target) | 1M × 50 | < 200s | < 300 MB |
| **xlstream 8-core** (target) | 1M × 50 | < 40s | < 300 MB |

## Checklist

### Benchmark crate

- [x] `benchmarks/Cargo.toml` — new workspace member.
- [x] `benchmarks/benches/` — criterion harnesses.
  - [x] `parse.rs` — formula parsing throughput.
  - [x] `arithmetic.rs` — `+`/`*`/`/` per-op throughput. 22-62M ops/sec measured.
  - [x] `lookup.rs` — hash lookup throughput on 1k and 10k-row tables.
  - [x] `aggregate.rs` — SUM/COUNT/AVERAGE over 10k and 100k columns.
  - [x] `end_to_end.rs` — full pipeline at small/medium tiers with RSS.
  - [x] `quick.rs` — smoke version that runs in CI per-PR (< 30 s). Self-contained fixture.
- [x] `benchmarks/fixtures/` — gitignored; generated by `generate-fixtures` binary.

### Fixture generation

- [x] `benchmarks/src/bin/generate_fixtures.rs` — deterministic (seeded) xlsx generation for all tiers.
- [x] Generator produces 3 xlsx files: `bench_small.xlsx`, `bench_medium.xlsx`, `bench_large.xlsx`.
- [x] Each includes 20 data cols + 30 formula cols covering every category in the table above.
- [x] Each includes 3 lookup sheets (Lookup1, Lookup2, Holidays).
- [x] Fixtures not committed (too large); regenerated by CI before benches run.
- [x] Correctness validated: small and medium fixtures evaluate without errors.

### Reference-workload bench

- [x] `end_to_end.rs` includes small and medium tiers.
- [x] Reports RSS + wall-clock per tier.
- [x] RSS asserted in bench (< 200 MB for small tier).

### CI integration

- [x] `.github/workflows/ci.yml` runs `cargo bench --bench quick -p xlstream-benchmarks` on every PR.
- [ ] `.github/workflows/nightly.yml` runs full bench suite; publishes with `benchmark-action/github-action-benchmark@v1`.
- [ ] PR regression threshold: 10% RSS, 20% wall-clock (requires nightly baseline).

### Comparative numbers

- [x] Document comparative results in `docs/research/benchmarks.md`.
- [x] Include formualizer baseline (5h 40m / 3.3 GB on 400k × 20).
- [x] Include xlstream measured numbers at each tier.
- [x] Parallel scaling curve recorded (1/2/4/8 workers on medium tier).

### Memory measurement

- [x] Integrate `memory-stats` crate for cross-platform RSS.
- [x] End-to-end bench asserts RSS within budget.

### Deferred items resolved

- [x] `2^3^2` right-associativity verified: parser is right-associative, `2^3^2 = 512`.
- [x] Arithmetic per-op throughput measured: 22-62M ops/sec.
- [x] Parallel scaling curve documented.

## Done when

- All three tiers pass their wall-clock and RSS assertions.
- Comparative numbers vs formualizer documented.
- Nightly CI publishes benchmark results to gh-pages.
- Per-PR smoke benchmarks run in < 30 s.
- Formula coverage: all 30 formula categories produce correct results in benchmark output.
